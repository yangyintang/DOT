# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'Decode Our Teacher' do
 
  use_frameworks!

  # Pods for Decode Our Teacher
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
end
